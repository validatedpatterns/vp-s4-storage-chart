{{- if .Values.s4UICredentials.vaultKey }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: s4-ui-credentials
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.s4Credentials.secretName }}
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: {{ .Values.s4UICredentials.vaultKey }}
{{- end }}
