{{- if .Values.s4Credentials.vaultKey }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.s4Credentials.secretName }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: {{ .Values.s4Credentials.secretName }}
  dataFrom:
    - extract:
        key: {{ .Values.s4Credentials.vaultKey }}
{{- end }}
